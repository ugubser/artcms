# Firebase Production Configuration
# Copy this file to .env.local and fill in your production Firebase credentials
# These values will be injected during build time

FIREBASE_API_KEY=your-production-api-key
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your-sender-id
FIREBASE_APP_ID=your-app-id

# Admin Email Configuration
# Comma-separated list of admin email addresses authorized for admin access
ADMIN_EMAILS=your-admin-email@example.com,another-admin@example.com

# Site URL for sitemap generation (used as base URL in sitemap.xml and sitemap.html)
SITE_URL=https://your-domain.com

# Note: Create these files during deployment:
# - .env.local (for local production testing)
# - .env.production (for CI/CD deployment)